ApiKeyRateLimitExceededEvent:
  type: object
  properties:
    event_id:
      type: string
    api_key_id:
      type: string
    owner_id:
      type: string
    endpoint:
      type: string
    requests_count:
      type: integer
      format: int32
    correlation_id:
      type: string
    timestamp:
      type: string
      format: date-time
  description: 'Kafka event: Rate limit exceeded'
