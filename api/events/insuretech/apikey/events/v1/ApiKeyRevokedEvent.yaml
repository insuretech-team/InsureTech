ApiKeyRevokedEvent:
  type: object
  properties:
    event_id:
      type: string
    api_key_id:
      type: string
    reason:
      type: string
    correlation_id:
      type: string
    timestamp:
      type: string
      format: date-time
  description: 'Kafka event: API key revoked'
