PasswordChangedEvent:
  type: object
  properties:
    event_id:
      type: string
    user_id:
      type: string
    timestamp:
      type: string
      format: date-time
    ip_address:
      type: string
    changed_by:
      type: string
      description: user_id or "admin"
  description: 'Event: Password changed'
