PasswordResetByEmailRequestedEvent:
  type: object
  properties:
    event_id:
      type: string
    user_id:
      type: string
    email_masked:
      type: string
    otp_id:
      type: string
    timestamp:
      type: string
      format: date-time
    ip_address:
      type: string
  description: 'Event: Password reset requested via email'
