AccessGrantedEvent:
  type: object
  properties:
    event_id:
      type: string
    user_id:
      type: string
    session_id:
      type: string
    domain:
      type: string
    subject:
      type: string
    object:
      type: string
    action:
      type: string
    matched_rule:
      type: string
    occurred_at:
      type: string
      format: date-time
  description: "AccessGrantedEvent \u2014 emitted for allowed decisions (optional,\
    \ configurable)"
