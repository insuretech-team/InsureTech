OrganisationApprovedEvent:
  type: object
  properties:
    event_id:
      type: string
    organisation_id:
      type: string
    approved_by:
      type: string
      description: SuperAdmin user_id
    timestamp:
      type: string
      format: date-time
  description: OrganisationApprovedEvent is published when SuperAdmin approves an
    organisation
