OrganisationSuspendedEvent:
  type: object
  properties:
    event_id:
      type: string
    organisation_id:
      type: string
    suspended_by:
      type: string
      description: SuperAdmin user_id
    reason:
      type: string
    timestamp:
      type: string
      format: date-time
  description: OrganisationSuspendedEvent is published when an organisation is suspended
