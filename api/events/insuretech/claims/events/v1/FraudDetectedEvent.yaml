ClaimsFraudDetectedEvent:
  type: object
  properties:
    event_id:
      type: string
    claim_id:
      type: string
    claim_number:
      type: string
    fraud_score:
      type: number
      format: double
    risk_indicators:
      type: array
      items:
        type: string
    timestamp:
      type: string
      format: date-time
    correlation_id:
      type: string
      description: 'CG-6: trace across services'
  description: 'Event: Fraud detected'
