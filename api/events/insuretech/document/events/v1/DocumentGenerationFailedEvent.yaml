DocumentGenerationFailedEvent:
  type: object
  properties:
    event_id:
      type: string
    document_generation_id:
      type: string
    error_message:
      type: string
    correlation_id:
      type: string
    timestamp:
      type: string
      format: date-time
  description: 'Kafka event: Document generation failed'
