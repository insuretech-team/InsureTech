PolicyIssuedEvent:
  type: object
  properties:
    event_id:
      type: string
    policy_id:
      type: string
    policy_number:
      type: string
    customer_id:
      type: string
    issued_at:
      type: string
      format: date-time
    effective_from:
      type: string
      format: date-time
    effective_to:
      type: string
      format: date-time
    correlation_id:
      type: string
      description: 'CG-6: trace across services'
  description: 'Event: Policy issued (after payment)'
