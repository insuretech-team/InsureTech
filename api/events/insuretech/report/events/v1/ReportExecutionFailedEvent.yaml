ReportExecutionFailedEvent:
  type: object
  properties:
    event_id:
      type: string
    report_execution_id:
      type: string
    error_message:
      type: string
    correlation_id:
      type: string
    timestamp:
      type: string
      format: date-time
  description: 'Kafka event: Report execution failed'
