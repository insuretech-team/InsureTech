TaskAssignedEvent:
  type: object
  properties:
    event_id:
      type: string
    task_id:
      type: string
    assigned_to:
      type: string
    correlation_id:
      type: string
    timestamp:
      type: string
      format: date-time
  description: 'Kafka event: Task assigned'
