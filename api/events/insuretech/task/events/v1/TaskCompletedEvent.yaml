TaskCompletedEvent:
  type: object
  properties:
    event_id:
      type: string
    task_id:
      type: string
    completed_by:
      type: string
    correlation_id:
      type: string
    timestamp:
      type: string
      format: date-time
  description: 'Kafka event: Task completed'
