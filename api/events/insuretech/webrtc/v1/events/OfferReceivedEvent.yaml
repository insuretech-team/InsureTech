OfferReceivedEvent:
  type: object
  properties:
    room_id:
      type: string
      description: ULID format
    offer:
      $ref: '#/components/schemas/SDPOffer'
    received_at:
      type: string
      format: date-time
  description: Event emitted when offer undergoes received. Published to event stream
    for downstream processing and audit trail.
