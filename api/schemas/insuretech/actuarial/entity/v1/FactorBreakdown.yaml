FactorBreakdown:
  type: object
  properties:
    factor_name:
      type: string
    factor_type:
      type: string
      description: RATING_FACTOR, LOADING, DISCOUNT
    factor_value:
      type: number
      format: double
    amount:
      type: number
      format: double
    description:
      type: string
  description: Domain entity representing factor breakdown. Core business object in
    the system.
