CalculationsListingResponse:
  type: object
  properties:
    calculations:
      type: array
      items:
        $ref: '#/components/schemas/ActuarialCalculation'
    total_count:
      type: integer
      format: int32
    next_page_token:
      type: string
  description: Response payload for calculations listing operation. Returns operation
    results and status.
