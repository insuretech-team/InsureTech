LossRatioCalculationsListingRequest:
  type: object
  properties:
    product_id:
      type: string
    line_of_business:
      type: string
    period_start:
      type: string
      format: date-time
    period_end:
      type: string
      format: date-time
    page_size:
      type: integer
      format: int32
    page_token:
      type: string
  description: Request payload for listing loss ratio calculations listing items.
    Supports filtering, sorting, and pagination.
