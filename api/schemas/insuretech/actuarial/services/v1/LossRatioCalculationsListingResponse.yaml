LossRatioCalculationsListingResponse:
  type: object
  properties:
    loss_ratios:
      type: array
      items:
        $ref: '#/components/schemas/LossRatioCalculation'
    total_count:
      type: integer
      format: int32
    next_page_token:
      type: string
  description: Response payload for loss ratio calculations listing operation. Returns
    operation results and status.
