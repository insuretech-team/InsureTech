RatingFormulasListingResponse:
  type: object
  properties:
    formulas:
      type: array
      items:
        $ref: '#/components/schemas/RatingFormula'
    total_count:
      type: integer
      format: int32
    next_page_token:
      type: string
  description: Response payload for rating formulas listing operation. Returns operation
    results and status.
