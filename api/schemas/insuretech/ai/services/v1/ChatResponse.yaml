ChatResponse:
  type: object
  properties:
    conversation_id:
      type: string
    suggested_actions:
      type: array
      items:
        type: string
    conversation_ended:
      type: boolean
  description: Response payload for chat operation. Returns operation results and
    status.
