DetectFraudRequest:
  type: object
  properties:
    claim_id:
      type: string
    policy_id:
      type: string
    claim_data:
      type: object
      additionalProperties:
        type: string
      description: Map field (key-value pairs)
  description: Request payload for detect fraud operation. Contains parameters required
    to execute the operation.
