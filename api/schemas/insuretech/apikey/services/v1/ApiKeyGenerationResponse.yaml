ApiKeyGenerationResponse:
  type: object
  properties:
    api_key_id:
      type: string
    api_key:
      type: string
      description: Plain text - show only once!
  description: Response payload for api key generation operation. Returns operation
    results and status.
