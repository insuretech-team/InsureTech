ApiKeyRetrievalResponse:
  type: object
  properties:
    api_key:
      $ref: '#/components/schemas/ApiKey'
  description: Response payload for api key retrieval operation. Returns operation
    results and status.
