RevokeApiKeyRequest:
  type: object
  properties:
    api_key_id:
      type: string
    reason:
      type: string
  description: Request payload for revoke api key operation. Contains parameters required
    to execute the operation.
