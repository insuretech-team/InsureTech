AuditEventCreationResponse:
  type: object
  properties:
    audit_event_id:
      type: string
  description: Response payload for audit event creation operation. Returns operation
    results and status.
