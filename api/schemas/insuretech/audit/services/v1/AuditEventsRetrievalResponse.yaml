AuditEventsRetrievalResponse:
  type: object
  properties:
    audit_events:
      type: array
      items:
        $ref: '#/components/schemas/AuditEvent'
    total_count:
      type: integer
      format: int32
  description: Response payload for audit events retrieval operation. Returns operation
    results and status.
