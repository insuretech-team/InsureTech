AuditLogsRetrievalResponse:
  type: object
  properties:
    audit_logs:
      type: array
      items:
        $ref: '#/components/schemas/AuditLog'
    total_count:
      type: integer
      format: int32
  description: Response payload for audit logs retrieval operation. Returns operation
    results and status.
