ComplianceLogCreationRequest:
  type: object
  properties:
    type:
      type: string
    regulation:
      type: string
    entity_type:
      type: string
    entity_id:
      type: string
    status:
      type: string
    description:
      type: string
    evidence:
      type: string
      description: JSON
  description: Request payload for creating a new compliance log creation. Contains
    all required fields and optional parameters for initialization.
