DLRStatusUpdateResponse:
  type: object
  properties:
    updated:
      type: boolean
    message:
      type: string
    error:
      $ref: '#/components/schemas/Error'
  description: Response payload for dlr status update operation. Returns operation
    results and status.
