EmailOTPSendingResponse:
  type: object
  properties:
    otp_id:
      type: string
    expires_in_seconds:
      type: integer
      format: int32
    cooldown_seconds:
      type: integer
      format: int32
  description: Response payload for email otp sending operation. Returns operation
    results and status.
