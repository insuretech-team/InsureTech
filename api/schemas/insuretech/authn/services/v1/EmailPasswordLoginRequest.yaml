EmailPasswordLoginRequest:
  type: object
  properties:
    email:
      type: string
    password:
      type: string
    device_id:
      type: string
    device_name:
      type: string
  description: Request payload for email password login operation. Contains parameters
    required to execute the operation.
