EmailVerificationResponse:
  type: object
  properties:
    verified:
      type: boolean
    user_id:
      type: string
  description: Response payload for email verification operation. Returns operation
    results and status.
