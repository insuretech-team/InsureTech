AuthnEndVoiceSessionResponse:
  type: object
  properties:
    message:
      type: string
    error:
      $ref: '#/components/schemas/Error'
  description: Response payload for end voice session operation. Returns operation
    results and status.
