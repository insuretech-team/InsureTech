InitiateVoiceSessionResponse:
  type: object
  properties:
    session_id:
      type: string
    challenge:
      type: string
    message:
      type: string
    error:
      $ref: '#/components/schemas/Error'
  description: Response payload for initiate voice session operation. Returns operation
    results and status.
