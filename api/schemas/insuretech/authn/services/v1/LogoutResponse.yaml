LogoutResponse:
  type: object
  properties:
    message:
      type: string
    session_revoked:
      type: boolean
      description: Indicates if session was successfully revoked
    error:
      $ref: '#/components/schemas/Error'
      description: Error information if operation failed
  description: Response payload for logout operation. Returns operation results and
    status.
