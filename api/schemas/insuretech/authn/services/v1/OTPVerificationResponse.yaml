OTPVerificationResponse:
  type: object
  properties:
    verified:
      type: boolean
    user_id:
      type: string
    message:
      type: string
    error:
      $ref: '#/components/schemas/Error'
      description: Error information if operation failed
  description: Response payload for otp verification operation. Returns operation
    results and status.
