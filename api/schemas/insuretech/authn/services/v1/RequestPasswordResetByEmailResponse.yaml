RequestPasswordResetByEmailResponse:
  type: object
  properties:
    otp_id:
      type: string
    expires_in_seconds:
      type: integer
      format: int32
  description: Response payload for request password reset by email operation. Returns
    operation results and status.
