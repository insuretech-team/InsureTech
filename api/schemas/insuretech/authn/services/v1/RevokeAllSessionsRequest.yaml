RevokeAllSessionsRequest:
  type: object
  properties:
    user_id:
      type: string
    reason:
      type: string
    exclude_current_session:
      type: boolean
      description: Keep current session active
  description: Revoke All Sessions Request
