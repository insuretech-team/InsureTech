RevokeAllSessionsResponse:
  type: object
  properties:
    revoked_count:
      type: integer
      format: int32
  description: Response payload for revoke all sessions operation. Returns operation
    results and status.
