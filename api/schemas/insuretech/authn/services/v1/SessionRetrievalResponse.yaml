SessionRetrievalResponse:
  type: object
  properties:
    session:
      $ref: '#/components/schemas/Session'
  description: Response payload for session retrieval operation. Returns operation
    results and status.
