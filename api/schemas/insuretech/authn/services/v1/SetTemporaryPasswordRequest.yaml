SetTemporaryPasswordRequest:
  type: object
  properties:
    user_id:
      type: string
    temporary_password:
      type: string
    assigned_by:
      type: string
    require_password_change:
      type: boolean
  description: Request payload for set temporary password operation. Contains parameters
    required to execute the operation.
