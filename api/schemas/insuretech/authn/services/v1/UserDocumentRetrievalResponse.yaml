UserDocumentRetrievalResponse:
  type: object
  properties:
    document:
      $ref: '#/components/schemas/UserDocument'
  description: Response payload for user document retrieval operation. Returns operation
    results and status.
