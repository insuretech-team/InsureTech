UserDocumentUpdateResponse:
  type: object
  properties:
    document:
      $ref: '#/components/schemas/UserDocument'
  description: Response payload for user document update operation. Returns operation
    results and status.
