UserDocumentUploadResponse:
  type: object
  properties:
    document:
      $ref: '#/components/schemas/UserDocument'
  description: Response payload for user document upload operation. Returns operation
    results and status.
