UserDocumentsListingRequest:
  type: object
  properties:
    user_id:
      type: string
    document_type_id:
      type: string
      description: filter
    page_size:
      type: integer
      format: int32
    page_token:
      type: string
  description: Request payload for listing user documents listing items. Supports
    filtering, sorting, and pagination.
