UserProfileCreationResponse:
  type: object
  properties:
    profile:
      $ref: '#/components/schemas/UserProfile'
  description: Response payload for user profile creation operation. Returns operation
    results and status.
