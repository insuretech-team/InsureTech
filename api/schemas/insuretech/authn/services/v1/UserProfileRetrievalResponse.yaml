UserProfileRetrievalResponse:
  type: object
  properties:
    profile:
      $ref: '#/components/schemas/UserProfile'
  description: Response payload for user profile retrieval operation. Returns operation
    results and status.
