VoiceSampleSubmissionResponse:
  type: object
  properties:
    verified:
      type: boolean
    message:
      type: string
    error:
      $ref: '#/components/schemas/Error'
  description: Response payload for voice sample submission operation. Returns operation
    results and status.
