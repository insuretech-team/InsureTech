VoiceSessionVerificationResponse:
  type: object
  properties:
    authenticated:
      type: boolean
    user_id:
      type: string
  description: Response payload for voice session verification operation. Returns
    operation results and status.
