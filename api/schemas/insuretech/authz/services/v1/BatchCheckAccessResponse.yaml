BatchCheckAccessResponse:
  type: object
  properties:
    results:
      type: array
      items:
        $ref: '#/components/schemas/AccessCheckResult'
  description: Response payload for batch check access operation. Returns operation
    results and status.
