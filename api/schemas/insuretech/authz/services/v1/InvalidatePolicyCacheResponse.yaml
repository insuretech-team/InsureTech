InvalidatePolicyCacheResponse:
  type: object
  properties:
    invalidated:
      type: boolean
  description: Response payload for invalidate policy cache operation. Returns operation
    results and status.
