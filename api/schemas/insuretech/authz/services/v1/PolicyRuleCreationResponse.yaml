PolicyRuleCreationResponse:
  type: object
  properties:
    policy:
      $ref: '#/components/schemas/PolicyRule'
  description: Response payload for policy rule creation operation. Returns operation
    results and status.
