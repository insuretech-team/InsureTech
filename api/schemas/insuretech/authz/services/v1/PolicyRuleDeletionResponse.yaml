PolicyRuleDeletionResponse:
  type: object
  properties:
    message:
      type: string
    error:
      $ref: '#/components/schemas/Error'
  description: Response payload for policy rule deletion operation. Returns operation
    results and status.
