PolicyRuleUpdateResponse:
  type: object
  properties:
    policy:
      $ref: '#/components/schemas/PolicyRule'
  description: Response payload for policy rule update operation. Returns operation
    results and status.
