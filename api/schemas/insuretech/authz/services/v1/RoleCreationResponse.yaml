RoleCreationResponse:
  type: object
  properties:
    role:
      $ref: '#/components/schemas/Role'
  description: Response payload for role creation operation. Returns operation results
    and status.
