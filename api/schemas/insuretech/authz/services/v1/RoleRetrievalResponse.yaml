RoleRetrievalResponse:
  type: object
  properties:
    role:
      $ref: '#/components/schemas/Role'
  description: Response payload for role retrieval operation. Returns operation results
    and status.
