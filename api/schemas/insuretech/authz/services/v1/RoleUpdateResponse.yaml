RoleUpdateResponse:
  type: object
  properties:
    role:
      $ref: '#/components/schemas/Role'
    error:
      $ref: '#/components/schemas/Error'
  description: Response payload for role update operation. Returns operation results
    and status.
