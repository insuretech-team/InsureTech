RolesListingRequest:
  type: object
  properties:
    portal:
      $ref: '#/components/schemas/Portal'
    active_only:
      type: boolean
    page_size:
      type: integer
      format: int32
    page_token:
      type: string
  description: Request payload for listing roles listing items. Supports filtering,
    sorting, and pagination.
