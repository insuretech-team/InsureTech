UserPermissionsRetrievalResponse:
  type: object
  properties:
    permissions:
      type: array
      items:
        $ref: '#/components/schemas/EffectivePermission'
        description: All effective (object, action) pairs the user is allowed in this
          domain
      description: All effective (object, action) pairs the user is allowed in this
        domain
    error:
      $ref: '#/components/schemas/Error'
  description: Response payload for user permissions retrieval operation. Returns
    operation results and status.
