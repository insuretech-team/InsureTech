AddOrgMemberResponse:
  type: object
  properties:
    member:
      $ref: '#/components/schemas/OrgMember'
  description: Response payload for add org member operation. Returns operation results
    and status.
