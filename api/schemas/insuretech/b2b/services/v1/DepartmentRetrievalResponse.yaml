DepartmentRetrievalResponse:
  type: object
  properties:
    department:
      $ref: '#/components/schemas/Department'
  description: Response payload for department retrieval operation. Returns operation
    results and status.
