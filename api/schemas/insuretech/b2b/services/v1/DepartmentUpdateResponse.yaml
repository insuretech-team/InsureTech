DepartmentUpdateResponse:
  type: object
  properties:
    department:
      $ref: '#/components/schemas/Department'
  description: Response payload for department update operation. Returns operation
    results and status.
