DepartmentsListingResponse:
  type: object
  properties:
    departments:
      type: array
      items:
        $ref: '#/components/schemas/Department'
    next_page_token:
      type: string
    total_count:
      type: integer
      format: int32
    error:
      $ref: '#/components/schemas/Error'
  description: Response payload for departments listing operation. Returns operation
    results and status.
