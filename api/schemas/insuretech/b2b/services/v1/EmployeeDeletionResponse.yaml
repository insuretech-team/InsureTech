EmployeeDeletionResponse:
  type: object
  properties:
    message:
      type: string
    error:
      $ref: '#/components/schemas/Error'
  description: Response payload for employee deletion operation. Returns operation
    results and status.
