EmployeeRetrievalResponse:
  type: object
  properties:
    employee:
      $ref: '#/components/schemas/EmployeeView'
  description: Response payload for employee retrieval operation. Returns operation
    results and status.
