EmployeesListingResponse:
  type: object
  properties:
    employees:
      type: array
      items:
        $ref: '#/components/schemas/EmployeeView'
    next_page_token:
      type: string
    total_count:
      type: integer
      format: int32
    error:
      $ref: '#/components/schemas/Error'
  description: Response payload for employees listing operation. Returns operation
    results and status.
