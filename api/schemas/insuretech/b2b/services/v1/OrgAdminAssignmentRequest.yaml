OrgAdminAssignmentRequest:
  type: object
  properties:
    organisation_id:
      type: string
    member_id:
      type: string
  description: Request payload for org admin assignment operation. Contains parameters
    required to execute the operation.
