OrganisationCreationResponse:
  type: object
  properties:
    organisation:
      $ref: '#/components/schemas/Organisation'
  description: Response payload for organisation creation operation. Returns operation
    results and status.
