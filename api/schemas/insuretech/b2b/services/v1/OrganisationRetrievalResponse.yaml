OrganisationRetrievalResponse:
  type: object
  properties:
    organisation:
      $ref: '#/components/schemas/Organisation'
  description: Response payload for organisation retrieval operation. Returns operation
    results and status.
