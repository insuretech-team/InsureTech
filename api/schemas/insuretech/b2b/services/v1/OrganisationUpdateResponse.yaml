OrganisationUpdateResponse:
  type: object
  properties:
    organisation:
      $ref: '#/components/schemas/Organisation'
  description: Response payload for organisation update operation. Returns operation
    results and status.
