PurchaseOrdersListingResponse:
  type: object
  properties:
    purchase_orders:
      type: array
      items:
        $ref: '#/components/schemas/PurchaseOrderView'
    next_page_token:
      type: string
    total_count:
      type: integer
      format: int32
  description: Response payload for purchase orders listing operation. Returns operation
    results and status.
