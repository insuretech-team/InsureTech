ResolveMyOrganisationRequest:
  type: object
  properties:
    user_id:
      type: string
      description: "user_id extracted from the validated session token (set by gateway,\
        \ NOT\n from client)"
  description: "ResolveMyOrganisation \u2014 called server-side (gateway) to resolve\
    \ business_id\n from user_id. Returns the organisation_id for the authenticated\
    \ user (via\n OrgMember lookup)."
