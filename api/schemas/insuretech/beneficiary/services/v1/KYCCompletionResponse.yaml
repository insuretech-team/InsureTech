KYCCompletionResponse:
  type: object
  properties:
    kyc_status:
      type: string
    message:
      type: string
    error:
      $ref: '#/components/schemas/Error'
      description: Error information if operation failed
  description: Response payload for kyc completion operation. Returns operation results
    and status.
