InvoiceCreationResponse:
  type: object
  properties:
    invoice:
      $ref: '#/components/schemas/Invoice'
  description: Response payload for invoice creation operation. Returns operation
    results and status.
