InvoicePDFGenerationResponse:
  type: object
  properties:
    invoice_id:
      type: string
    job_id:
      type: string
      description: Async generation job ID
  description: Response payload for invoice pdf generation operation. Returns operation
    results and status.
