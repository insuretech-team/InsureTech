InvoicePDFRetrievalResponse:
  type: object
  properties:
    invoice_id:
      type: string
    invoice_pdf_url:
      type: string
      description: Pre-signed URL
    generated_at:
      type: string
      format: date-time
  description: Response payload for invoice pdf retrieval operation. Returns operation
    results and status.
