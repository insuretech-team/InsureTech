InvoiceRetrievalResponse:
  type: object
  properties:
    invoice:
      $ref: '#/components/schemas/Invoice'
  description: Response payload for invoice retrieval operation. Returns operation
    results and status.
