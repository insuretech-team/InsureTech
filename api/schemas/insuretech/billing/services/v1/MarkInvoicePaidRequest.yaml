MarkInvoicePaidRequest:
  type: object
  properties:
    invoice_id:
      type: string
    payment_id:
      type: string
    paid_at:
      type: string
      format: date-time
  description: Request payload for mark invoice paid operation. Contains parameters
    required to execute the operation.
