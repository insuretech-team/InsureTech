MarkInvoicePaidResponse:
  type: object
  properties:
    invoice_id:
      type: string
    status:
      $ref: '#/components/schemas/InvoiceStatus'
  description: Response payload for mark invoice paid operation. Returns operation
    results and status.
