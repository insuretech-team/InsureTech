ClaimApprovalRequest:
  type: object
  properties:
    claim_id:
      type: string
    approver_id:
      type: string
    approved_amount:
      $ref: '#/components/schemas/Money'
    notes:
      type: string
  description: Request payload for claim approval operation. Contains parameters required
    to execute the operation.
