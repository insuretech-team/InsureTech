ClaimRejectionRequest:
  type: object
  properties:
    claim_id:
      type: string
    approver_id:
      type: string
    reason:
      type: string
  description: Request payload for claim rejection operation. Contains parameters
    required to execute the operation.
