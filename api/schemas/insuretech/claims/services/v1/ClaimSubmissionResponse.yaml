ClaimSubmissionResponse:
  type: object
  properties:
    claim_id:
      type: string
    claim_number:
      type: string
    message:
      type: string
    error:
      $ref: '#/components/schemas/Error'
      description: Error information if operation failed
  description: Response payload for claim submission operation. Returns operation
    results and status.
