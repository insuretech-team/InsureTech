DisputeClaimResponse:
  type: object
  properties:
    dispute_id:
      type: string
    message:
      type: string
    error:
      $ref: '#/components/schemas/Error'
  description: Response payload for dispute claim operation. Returns operation results
    and status.
