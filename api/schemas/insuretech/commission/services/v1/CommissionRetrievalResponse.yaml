CommissionRetrievalResponse:
  type: object
  properties:
    commission:
      $ref: '#/components/schemas/Commission'
  description: Response payload for commission retrieval operation. Returns operation
    results and status.
