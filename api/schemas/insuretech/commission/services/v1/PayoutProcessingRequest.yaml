PayoutProcessingRequest:
  type: object
  properties:
    payout_id:
      type: string
    payment_method:
      type: string
    payment_reference:
      type: string
  description: Request payload for payout processing operation. Contains parameters
    required to execute the operation.
