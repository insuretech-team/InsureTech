PageRequest:
  type: object
  properties:
    page:
      type: integer
      format: int32
      description: 1-based
    page_size:
      type: integer
      format: int32
      description: items per page
    page_token:
      type: string
      description: alt token-based pagination
  description: Pagination helpers
