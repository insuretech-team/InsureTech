Response:
  type: object
  properties:
    success:
      type: boolean
    error_code:
      type: string
    metadata:
      type: object
      additionalProperties:
        type: string
      description: Map field (key-value pairs)
  description: Generic response wrapper
