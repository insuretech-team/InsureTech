ContactRetrievalRequest:
  type: object
  properties:
    contact_id:
      type: string
  description: Request payload for retrieving contact retrieval information. May include
    filters and pagination parameters.
