LeadDeletionRequest:
  type: object
  properties:
    lead_id:
      type: string
    permanent:
      type: boolean
  description: Request payload for lead deletion operation. Contains parameters required
    to execute the operation.
