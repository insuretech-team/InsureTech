DocumentRetrievalResponse:
  type: object
  properties:
    document:
      $ref: '#/components/schemas/DocumentGeneration'
  description: Response payload for document retrieval operation. Returns operation
    results and status.
