DocumentTemplateDeactivationRequest:
  type: object
  properties:
    template_id:
      type: string
    reason:
      type: string
  description: Request payload for document template deactivation operation. Contains
    parameters required to execute the operation.
