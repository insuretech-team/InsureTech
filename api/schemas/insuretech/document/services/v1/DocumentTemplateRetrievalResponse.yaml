DocumentTemplateRetrievalResponse:
  type: object
  properties:
    template:
      $ref: '#/components/schemas/DocumentTemplate'
  description: Response payload for document template retrieval operation. Returns
    operation results and status.
