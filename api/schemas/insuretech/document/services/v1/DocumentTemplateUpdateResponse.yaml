DocumentTemplateUpdateResponse:
  type: object
  properties:
    message:
      type: string
    error:
      $ref: '#/components/schemas/Error'
  description: Response payload for document template update operation. Returns operation
    results and status.
