DocumentsListingResponse:
  type: object
  properties:
    documents:
      type: array
      items:
        $ref: '#/components/schemas/DocumentGeneration'
    total_count:
      type: integer
      format: int32
  description: Response payload for documents listing operation. Returns operation
    results and status.
