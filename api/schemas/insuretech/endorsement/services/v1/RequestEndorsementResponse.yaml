RequestEndorsementResponse:
  type: object
  properties:
    endorsement_id:
      type: string
    endorsement_number:
      type: string
  description: Response payload for request endorsement operation. Returns operation
    results and status.
