FraudAlertsListingRequest:
  type: object
  properties:
    status:
      type: string
    risk_level:
      type: string
    start_date:
      type: string
    end_date:
      type: string
    page:
      type: integer
      format: int32
    page_size:
      type: integer
      format: int32
  description: Request payload for listing fraud alerts listing items. Supports filtering,
    sorting, and pagination.
