FraudAlertsListingResponse:
  type: object
  properties:
    fraud_alerts:
      type: array
      items:
        $ref: '#/components/schemas/FraudAlert'
    total_count:
      type: integer
      format: int32
    error:
      $ref: '#/components/schemas/Error'
      description: Error information if operation failed
  description: Response payload for fraud alerts listing operation. Returns operation
    results and status.
