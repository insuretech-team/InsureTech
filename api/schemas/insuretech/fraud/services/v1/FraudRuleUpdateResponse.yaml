FraudFraudRuleUpdateResponse:
  type: object
  properties:
    message:
      type: string
    error:
      $ref: '#/components/schemas/Error'
  description: Response payload for fraud rule update operation. Returns operation
    results and status.
