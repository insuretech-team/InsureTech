FraudFraudRulesListingResponse:
  type: object
  properties:
    fraud_rules:
      type: array
      items:
        $ref: '#/components/schemas/FraudRule'
    next_page_token:
      type: string
    total_count:
      type: integer
      format: int32
    error:
      $ref: '#/components/schemas/Error'
      description: Error information if operation failed
  description: Response payload for fraud rules listing operation. Returns operation
    results and status.
