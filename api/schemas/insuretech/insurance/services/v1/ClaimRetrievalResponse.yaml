InsuranceClaimRetrievalResponse:
  type: object
  properties:
    claim:
      $ref: '#/components/schemas/Claim'
  description: Response payload for claim retrieval operation. Returns operation results
    and status.
