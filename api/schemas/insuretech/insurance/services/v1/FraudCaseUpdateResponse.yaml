InsuranceFraudCaseUpdateResponse:
  type: object
  properties:
    fraud_case:
      $ref: '#/components/schemas/FraudCase'
  description: Response payload for fraud case update operation. Returns operation
    results and status.
