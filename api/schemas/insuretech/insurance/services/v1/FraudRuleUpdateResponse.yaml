InsuranceFraudRuleUpdateResponse:
  type: object
  properties:
    rule:
      $ref: '#/components/schemas/FraudRule'
  description: Response payload for fraud rule update operation. Returns operation
    results and status.
