GracePeriodCreationResponse:
  type: object
  properties:
    grace_period:
      $ref: '#/components/schemas/GracePeriod'
  description: Response payload for grace period creation operation. Returns operation
    results and status.
