InsuranceProposalCreationResponse:
  type: object
  properties:
    proposal:
      $ref: '#/components/schemas/InsuranceProposal'
  description: Response payload for insurance proposal creation operation. Returns
    operation results and status.
