InsuranceProposalRetrievalResponse:
  type: object
  properties:
    proposal:
      $ref: '#/components/schemas/InsuranceProposal'
  description: Response payload for insurance proposal retrieval operation. Returns
    operation results and status.
