InsurerConfigByInsurerRetrievalResponse:
  type: object
  properties:
    config:
      $ref: '#/components/schemas/InsurerConfig'
  description: Response payload for insurer config by insurer retrieval operation.
    Returns operation results and status.
