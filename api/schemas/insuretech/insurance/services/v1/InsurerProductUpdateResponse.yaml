InsuranceInsurerProductUpdateResponse:
  type: object
  properties:
    product:
      $ref: '#/components/schemas/InsurerProduct'
  description: Response payload for insurer product update operation. Returns operation
    results and status.
