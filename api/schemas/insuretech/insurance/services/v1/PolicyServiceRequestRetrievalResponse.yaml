PolicyServiceRequestRetrievalResponse:
  type: object
  properties:
    request:
      $ref: '#/components/schemas/PolicyServiceRequest'
  description: Response payload for policy service request retrieval operation. Returns
    operation results and status.
