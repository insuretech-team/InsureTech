PricingConfigsListingRequest:
  type: object
  properties:
    product_id:
      type: string
  description: Request payload for listing pricing configs listing items. Supports
    filtering, sorting, and pagination.
