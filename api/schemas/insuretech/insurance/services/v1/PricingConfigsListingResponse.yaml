PricingConfigsListingResponse:
  type: object
  properties:
    configs:
      type: array
      items:
        $ref: '#/components/schemas/PricingConfig'
  description: Response payload for pricing configs listing operation. Returns operation
    results and status.
