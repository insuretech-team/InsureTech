InsuranceProductCreationResponse:
  type: object
  properties:
    product:
      $ref: '#/components/schemas/Product'
  description: Response payload for product creation operation. Returns operation
    results and status.
