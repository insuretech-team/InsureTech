ProductPlanRetrievalResponse:
  type: object
  properties:
    plan:
      $ref: '#/components/schemas/ProductPlan'
  description: Response payload for product plan retrieval operation. Returns operation
    results and status.
