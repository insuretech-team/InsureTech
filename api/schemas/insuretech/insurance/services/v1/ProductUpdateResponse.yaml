InsuranceProductUpdateResponse:
  type: object
  properties:
    product:
      $ref: '#/components/schemas/Product'
  description: Response payload for product update operation. Returns operation results
    and status.
