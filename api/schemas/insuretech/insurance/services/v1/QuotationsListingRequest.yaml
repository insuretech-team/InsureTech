QuotationsListingRequest:
  type: object
  properties:
    business_id:
      type: string
    page:
      type: integer
      format: int32
    page_size:
      type: integer
      format: int32
  description: Request payload for listing quotations listing items. Supports filtering,
    sorting, and pagination.
