InsuranceQuoteRetrievalResponse:
  type: object
  properties:
    quote:
      $ref: '#/components/schemas/UnderwritingQuote'
  description: Response payload for quote retrieval operation. Returns operation results
    and status.
