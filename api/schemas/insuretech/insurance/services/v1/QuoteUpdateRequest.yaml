QuoteUpdateRequest:
  type: object
  properties:
    quote:
      $ref: '#/components/schemas/Quote'
  description: Request payload for updating an existing quote update. Contains fields
    to be modified.
