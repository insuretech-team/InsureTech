RenewalSchedulesListingRequest:
  type: object
  properties:
    policy_id:
      type: string
  description: Request payload for listing renewal schedules listing items. Supports
    filtering, sorting, and pagination.
