RidersListingRequest:
  type: object
  properties:
    product_id:
      type: string
  description: Request payload for listing riders listing items. Supports filtering,
    sorting, and pagination.
