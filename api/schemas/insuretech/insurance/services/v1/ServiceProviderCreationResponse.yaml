ServiceProviderCreationResponse:
  type: object
  properties:
    provider:
      $ref: '#/components/schemas/ServiceProvider'
  description: Response payload for service provider creation operation. Returns operation
    results and status.
