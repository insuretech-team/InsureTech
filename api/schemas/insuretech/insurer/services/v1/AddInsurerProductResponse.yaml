AddInsurerProductResponse:
  type: object
  properties:
    insurer_product_id:
      type: string
    message:
      type: string
    error:
      $ref: '#/components/schemas/Error'
      description: Error information if operation failed
  description: Response payload for add insurer product operation. Returns operation
    results and status.
