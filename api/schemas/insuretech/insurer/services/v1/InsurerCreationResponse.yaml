InsurerInsurerCreationResponse:
  type: object
  properties:
    insurer_id:
      type: string
    message:
      type: string
    error:
      $ref: '#/components/schemas/Error'
      description: Error information if operation failed
  description: Response payload for insurer creation operation. Returns operation
    results and status.
