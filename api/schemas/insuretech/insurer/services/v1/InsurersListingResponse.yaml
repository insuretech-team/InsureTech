InsurerInsurersListingResponse:
  type: object
  properties:
    insurers:
      type: array
      items:
        $ref: '#/components/schemas/Insurer'
    total_count:
      type: integer
      format: int32
    error:
      $ref: '#/components/schemas/Error'
      description: Error information if operation failed
  description: Response payload for insurers listing operation. Returns operation
    results and status.
