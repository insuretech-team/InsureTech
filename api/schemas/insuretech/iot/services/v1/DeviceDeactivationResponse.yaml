DeviceDeactivationResponse:
  type: object
  properties:
    message:
      type: string
    error:
      $ref: '#/components/schemas/Error'
      description: Error information if operation failed
  description: Response payload for device deactivation operation. Returns operation
    results and status.
