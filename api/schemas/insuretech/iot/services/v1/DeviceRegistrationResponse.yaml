DeviceRegistrationResponse:
  type: object
  properties:
    device_id:
      type: string
    activation_token:
      type: string
  description: Response payload for device registration operation. Returns operation
    results and status.
