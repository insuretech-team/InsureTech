StreamTelemetryResponse:
  type: object
  properties:
    telemetry_id:
      type: string
    received:
      type: boolean
  description: Response payload for stream telemetry operation. Returns operation
    results and status.
