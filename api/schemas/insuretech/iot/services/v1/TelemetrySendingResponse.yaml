TelemetrySendingResponse:
  type: object
  properties:
    telemetry_id:
      type: string
    accepted:
      type: boolean
  description: Response payload for telemetry sending operation. Returns operation
    results and status.
