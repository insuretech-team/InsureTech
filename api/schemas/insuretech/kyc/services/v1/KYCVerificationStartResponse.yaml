KYCVerificationStartResponse:
  type: object
  properties:
    kyc_verification_id:
      type: string
    message:
      type: string
    error:
      $ref: '#/components/schemas/Error'
      description: Error information if operation failed
  description: Response payload for kyc verification start operation. Returns operation
    results and status.
