PendingVerificationsListingResponse:
  type: object
  properties:
    verifications:
      type: array
      items:
        $ref: '#/components/schemas/KYCVerification'
    next_page_token:
      type: string
    total_count:
      type: integer
      format: int32
    error:
      $ref: '#/components/schemas/Error'
  description: Response payload for pending verifications listing operation. Returns
    operation results and status.
