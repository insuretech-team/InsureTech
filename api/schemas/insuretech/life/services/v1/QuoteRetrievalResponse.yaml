LifeQuoteRetrievalResponse:
  type: object
  properties:
    quote:
      $ref: '#/components/schemas/LifeQuote'
  description: Response payload for quote retrieval operation. Returns operation results
    and status.
