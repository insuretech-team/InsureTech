LifeQuotesListingRequest:
  type: object
  properties:
    customer_id:
      type: string
    product_id:
      type: string
    status:
      $ref: '#/components/schemas/LifeQuoteStatus'
    page_size:
      type: integer
      format: int32
    page_token:
      type: string
  description: Request payload for listing quotes listing items. Supports filtering,
    sorting, and pagination.
