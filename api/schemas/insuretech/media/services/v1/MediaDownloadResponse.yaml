MediaDownloadResponse:
  type: object
  properties:
    download_url:
      type: string
      description: Signed URL
    expires_in_seconds:
      type: string
      format: int64
  description: Response payload for media download operation. Returns operation results
    and status.
