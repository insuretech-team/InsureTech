MediaListingResponse:
  type: object
  properties:
    media_files:
      type: array
      items:
        $ref: '#/components/schemas/MediaFile'
    total_count:
      type: integer
      format: int32
  description: Response payload for media listing operation. Returns operation results
    and status.
