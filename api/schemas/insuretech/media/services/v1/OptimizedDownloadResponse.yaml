OptimizedDownloadResponse:
  type: object
  properties:
    download_url:
      type: string
    expires_in_seconds:
      type: string
      format: int64
    error:
      $ref: '#/components/schemas/Error'
  description: Response payload for optimized download operation. Returns operation
    results and status.
