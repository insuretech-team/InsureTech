ProcessingJobsListingResponse:
  type: object
  properties:
    jobs:
      type: array
      items:
        $ref: '#/components/schemas/ingJobProcessing'
    total_count:
      type: integer
      format: int32
    error:
      $ref: '#/components/schemas/Error'
  description: Response payload for processing jobs listing operation. Returns operation
    results and status.
