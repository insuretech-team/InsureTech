RequestProcessingResponse:
  type: object
  properties:
    job_id:
      type: string
    status:
      type: string
  description: Response payload for request processing operation. Returns operation
    results and status.
