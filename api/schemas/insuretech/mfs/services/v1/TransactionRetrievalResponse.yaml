TransactionRetrievalResponse:
  type: object
  properties:
    transaction:
      $ref: '#/components/schemas/MFSTransaction'
  description: Response payload for transaction retrieval operation. Returns operation
    results and status.
