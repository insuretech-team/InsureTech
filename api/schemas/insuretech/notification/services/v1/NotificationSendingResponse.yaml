NotificationSendingResponse:
  type: object
  properties:
    notification_id:
      type: string
  description: Response payload for notification sending operation. Returns operation
    results and status.
