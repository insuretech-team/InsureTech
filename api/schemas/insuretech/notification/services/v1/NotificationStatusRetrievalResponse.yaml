NotificationStatusRetrievalResponse:
  type: object
  properties:
    notification:
      $ref: '#/components/schemas/Notification'
  description: Response payload for notification status retrieval operation. Returns
    operation results and status.
