NotificationTemplateCreationResponse:
  type: object
  properties:
    template_id:
      type: string
  description: Response payload for notification template creation operation. Returns
    operation results and status.
