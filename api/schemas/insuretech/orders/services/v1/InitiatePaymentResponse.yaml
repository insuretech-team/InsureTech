OrdersInitiatePaymentResponse:
  type: object
  properties:
    order_id:
      type: string
    payment_id:
      type: string
    payment_url:
      type: string
    payment_gateway_ref:
      type: string
    expires_at:
      type: string
      format: date-time
  description: Response payload for initiate payment operation. Returns operation
    results and status.
