OrderCreationResponse:
  type: object
  properties:
    order:
      $ref: '#/components/schemas/OrderView'
  description: Response payload for order creation operation. Returns operation results
    and status.
