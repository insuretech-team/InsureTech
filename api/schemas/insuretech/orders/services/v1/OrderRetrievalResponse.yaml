OrderRetrievalResponse:
  type: object
  properties:
    order:
      $ref: '#/components/schemas/OrderView'
  description: Response payload for order retrieval operation. Returns operation results
    and status.
