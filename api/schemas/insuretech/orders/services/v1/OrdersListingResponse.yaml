OrdersListingResponse:
  type: object
  properties:
    orders:
      type: array
      items:
        $ref: '#/components/schemas/OrderView'
    next_page_token:
      type: string
    total_count:
      type: integer
      format: int32
    error:
      $ref: '#/components/schemas/Error'
  description: Response payload for orders listing operation. Returns operation results
    and status.
