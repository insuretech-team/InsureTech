PaymentConfirmationResponse:
  type: object
  properties:
    order_id:
      type: string
    status:
      $ref: '#/components/schemas/OrderStatus'
  description: Response payload for payment confirmation operation. Returns operation
    results and status.
