CommissionStructureUpdateResponse:
  type: object
  properties:
    success:
      type: boolean
  description: Response payload for commission structure update operation. Returns
    operation results and status.
