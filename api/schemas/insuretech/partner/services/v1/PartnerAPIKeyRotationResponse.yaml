PartnerAPIKeyRotationResponse:
  type: object
  properties:
    new_api_key:
      type: string
    new_api_secret:
      type: string
    expires_at:
      type: string
      format: date-time
  description: Response payload for partner api key rotation operation. Returns operation
    results and status.
