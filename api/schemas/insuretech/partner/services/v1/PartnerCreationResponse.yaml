PartnerCreationResponse:
  type: object
  properties:
    partner_id:
      type: string
    partner:
      $ref: '#/components/schemas/Partner'
  description: Response payload for partner creation operation. Returns operation
    results and status.
