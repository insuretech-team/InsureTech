PartnerRetrievalResponse:
  type: object
  properties:
    partner:
      $ref: '#/components/schemas/Partner'
    error:
      $ref: '#/components/schemas/Error'
      description: Error information if operation failed
  description: Response payload for partner retrieval operation. Returns operation
    results and status.
