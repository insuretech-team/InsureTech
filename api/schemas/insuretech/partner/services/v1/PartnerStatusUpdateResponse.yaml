PartnerStatusUpdateResponse:
  type: object
  properties:
    partner:
      $ref: '#/components/schemas/Partner'
  description: Response payload for partner status update operation. Returns operation
    results and status.
