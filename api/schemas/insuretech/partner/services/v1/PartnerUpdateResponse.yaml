PartnerUpdateResponse:
  type: object
  properties:
    partner:
      $ref: '#/components/schemas/Partner'
  description: Response payload for partner update operation. Returns operation results
    and status.
