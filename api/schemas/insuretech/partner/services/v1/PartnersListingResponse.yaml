PartnersListingResponse:
  type: object
  properties:
    partners:
      type: array
      items:
        $ref: '#/components/schemas/Partner'
    next_page_token:
      type: string
    total_count:
      type: integer
      format: int32
  description: Response payload for partners listing operation. Returns operation
    results and status.
