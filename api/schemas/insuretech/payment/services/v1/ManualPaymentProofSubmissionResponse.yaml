ManualPaymentProofSubmissionResponse:
  type: object
  properties:
    payment_id:
      type: string
    status:
      type: string
    error:
      $ref: '#/components/schemas/Error'
  description: Response payload for manual payment proof submission operation. Returns
    operation results and status.
