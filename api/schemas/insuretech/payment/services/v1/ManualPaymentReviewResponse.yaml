ManualPaymentReviewResponse:
  type: object
  properties:
    payment_id:
      type: string
    status:
      type: string
  description: Response payload for manual payment review operation. Returns operation
    results and status.
