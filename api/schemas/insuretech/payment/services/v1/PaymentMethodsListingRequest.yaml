PaymentMethodsListingRequest:
  type: object
  properties:
    user_id:
      type: string
    page_size:
      type: integer
      format: int32
    page_token:
      type: string
  description: Request payload for listing payment methods listing items. Supports
    filtering, sorting, and pagination.
