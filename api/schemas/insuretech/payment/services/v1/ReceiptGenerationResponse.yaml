ReceiptGenerationResponse:
  type: object
  properties:
    payment_id:
      type: string
    receipt_number:
      type: string
    receipt_file_id:
      type: string
  description: Response payload for receipt generation operation. Returns operation
    results and status.
