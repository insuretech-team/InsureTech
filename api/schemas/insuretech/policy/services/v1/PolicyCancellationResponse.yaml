PolicyCancellationResponse:
  type: object
  properties:
    refund_amount:
      $ref: '#/components/schemas/Money'
  description: Response payload for policy cancellation operation. Returns operation
    results and status.
