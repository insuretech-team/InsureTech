PolicyPolicyCreationResponse:
  type: object
  properties:
    policy_id:
      type: string
    policy_number:
      type: string
    message:
      type: string
    error:
      $ref: '#/components/schemas/Error'
      description: Error information if operation failed
  description: Response payload for policy creation operation. Returns operation results
    and status.
