PolicyDocumentGenerationResponse:
  type: object
  properties:
    document_url:
      type: string
    qr_code:
      type: string
  description: Response payload for policy document generation operation. Returns
    operation results and status.
