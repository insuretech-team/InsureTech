PolicyIssuanceResponse:
  type: object
  properties:
    policy:
      $ref: '#/components/schemas/Policy'
  description: Response payload for policy issuance operation. Returns operation results
    and status.
