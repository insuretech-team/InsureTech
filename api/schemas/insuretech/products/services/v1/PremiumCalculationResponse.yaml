ProductsPremiumCalculationResponse:
  type: object
  properties:
    base_premium:
      $ref: '#/components/schemas/Money'
    rider_premium:
      $ref: '#/components/schemas/Money'
    total_premium:
      $ref: '#/components/schemas/Money'
    breakdown:
      type: array
      items:
        $ref: '#/components/schemas/ProductsPremiumBreakdown'
  description: Response payload for premium calculation operation. Returns operation
    results and status.
