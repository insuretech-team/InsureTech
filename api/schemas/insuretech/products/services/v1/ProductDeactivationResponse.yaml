ProductDeactivationResponse:
  type: object
  properties:
    message:
      type: string
    error:
      $ref: '#/components/schemas/Error'
  description: Response payload for product deactivation operation. Returns operation
    results and status.
