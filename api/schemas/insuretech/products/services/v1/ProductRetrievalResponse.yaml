ProductsProductRetrievalResponse:
  type: object
  properties:
    product:
      $ref: '#/components/schemas/Product'
    error:
      $ref: '#/components/schemas/Error'
      description: Error information if operation failed
  description: Response payload for product retrieval operation. Returns operation
    results and status.
