DiscountComparison:
  type: object
  properties:
    discount_id:
      type: string
    name:
      type: string
    amount:
      $ref: '#/components/schemas/Money'
    percentage:
      type: number
      format: double
  description: Domain entity representing discount comparison. Core business object
    in the system.
