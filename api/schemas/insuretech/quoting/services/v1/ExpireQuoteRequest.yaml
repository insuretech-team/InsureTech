ExpireQuoteRequest:
  type: object
  properties:
    quote_id:
      type: string
  description: Request payload for expire quote operation. Contains parameters required
    to execute the operation.
