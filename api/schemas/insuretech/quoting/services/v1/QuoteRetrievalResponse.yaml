QuotingQuoteRetrievalResponse:
  type: object
  properties:
    quote:
      $ref: '#/components/schemas/QuotingQuote'
  description: Response payload for quote retrieval operation. Returns operation results
    and status.
