QuotingQuotesListingResponse:
  type: object
  properties:
    quotes:
      type: array
      items:
        $ref: '#/components/schemas/QuotingQuote'
    next_page_token:
      type: string
    total_count:
      type: integer
      format: int32
  description: Response payload for quotes listing operation. Returns operation results
    and status.
