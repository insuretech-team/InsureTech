RefundProcessingResponse:
  type: object
  properties:
    message:
      type: string
    processed_at:
      type: string
    error:
      $ref: '#/components/schemas/Error'
      description: Error information if operation failed
  description: Response payload for refund processing operation. Returns operation
    results and status.
