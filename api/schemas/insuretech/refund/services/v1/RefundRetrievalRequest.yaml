RefundRetrievalRequest:
  type: object
  properties:
    refund_id:
      type: string
  description: Request payload for retrieving refund retrieval information. May include
    filters and pagination parameters.
