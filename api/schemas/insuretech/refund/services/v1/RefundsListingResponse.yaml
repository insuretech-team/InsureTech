RefundsListingResponse:
  type: object
  properties:
    refunds:
      type: array
      items:
        $ref: '#/components/schemas/Refund'
    total_count:
      type: integer
      format: int32
  description: Response payload for refunds listing operation. Returns operation results
    and status.
