RequestRefundResponse:
  type: object
  properties:
    refund_id:
      type: string
    refund_number:
      type: string
  description: Response payload for request refund operation. Returns operation results
    and status.
