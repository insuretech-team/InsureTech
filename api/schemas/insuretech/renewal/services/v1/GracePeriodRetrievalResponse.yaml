RenewalGracePeriodRetrievalResponse:
  type: object
  properties:
    grace_period:
      $ref: '#/components/schemas/GracePeriod'
    error:
      $ref: '#/components/schemas/Error'
      description: Error information if operation failed
  description: Response payload for grace period retrieval operation. Returns operation
    results and status.
