RenewalPolicyRenewalResponse:
  type: object
  properties:
    new_policy_id:
      type: string
    message:
      type: string
    error:
      $ref: '#/components/schemas/Error'
      description: Error information if operation failed
  description: Response payload for policy renewal operation. Returns operation results
    and status.
