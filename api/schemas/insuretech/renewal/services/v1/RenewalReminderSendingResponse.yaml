RenewalReminderSendingResponse:
  type: object
  properties:
    reminder_id:
      type: string
  description: Response payload for renewal reminder sending operation. Returns operation
    results and status.
