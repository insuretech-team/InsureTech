RenewalRenewalScheduleRetrievalResponse:
  type: object
  properties:
    renewal_schedule:
      $ref: '#/components/schemas/RenewalSchedule'
    reminders:
      type: array
      items:
        $ref: '#/components/schemas/RenewalReminder'
  description: Response payload for renewal schedule retrieval operation. Returns
    operation results and status.
