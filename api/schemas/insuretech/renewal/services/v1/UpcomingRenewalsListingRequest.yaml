UpcomingRenewalsListingRequest:
  type: object
  properties:
    days_ahead:
      type: integer
      format: int32
    status:
      type: string
    page:
      type: integer
      format: int32
    page_size:
      type: integer
      format: int32
  description: Request payload for listing upcoming renewals listing items. Supports
    filtering, sorting, and pagination.
