ReportDefinitionsListingRequest:
  type: object
  properties:
    category:
      type: string
    active_only:
      type: boolean
    page_size:
      type: integer
      format: int32
    page_token:
      type: string
  description: Request payload for listing report definitions listing items. Supports
    filtering, sorting, and pagination.
