ReportExecutionRetrievalResponse:
  type: object
  properties:
    report_execution:
      $ref: '#/components/schemas/ReportExecution'
  description: Response payload for report execution retrieval operation. Returns
    operation results and status.
