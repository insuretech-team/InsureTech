ReportExecutionsListingRequest:
  type: object
  properties:
    report_definition_id:
      type: string
    status:
      type: string
    start_date:
      type: string
    end_date:
      type: string
    page:
      type: integer
      format: int32
    page_size:
      type: integer
      format: int32
  description: Request payload for listing report executions listing items. Supports
    filtering, sorting, and pagination.
