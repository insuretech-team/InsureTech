AddTicketMessageResponse:
  type: object
  properties:
    message_id:
      type: string
    message:
      type: string
    error:
      $ref: '#/components/schemas/Error'
      description: Error information if operation failed
  description: Response payload for add ticket message operation. Returns operation
    results and status.
