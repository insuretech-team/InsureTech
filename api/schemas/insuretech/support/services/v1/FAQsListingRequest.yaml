FAQsListingRequest:
  type: object
  properties:
    category:
      type: string
    language:
      type: string
      description: en, bn
    page_size:
      type: integer
      format: int32
    page_token:
      type: string
  description: Request payload for listing fa qs listing items. Supports filtering,
    sorting, and pagination.
