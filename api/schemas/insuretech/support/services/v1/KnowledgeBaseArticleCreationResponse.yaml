KnowledgeBaseArticleCreationResponse:
  type: object
  properties:
    article_id:
      type: string
    message:
      type: string
    error:
      $ref: '#/components/schemas/Error'
  description: Response payload for knowledge base article creation operation. Returns
    operation results and status.
