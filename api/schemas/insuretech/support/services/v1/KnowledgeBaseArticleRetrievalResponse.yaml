KnowledgeBaseArticleRetrievalResponse:
  type: object
  properties:
    article:
      $ref: '#/components/schemas/KnowledgeBaseArticle'
    error:
      $ref: '#/components/schemas/Error'
      description: Error information if operation failed
  description: Response payload for knowledge base article retrieval operation. Returns
    operation results and status.
