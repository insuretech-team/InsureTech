KnowledgeBaseSearchResponse:
  type: object
  properties:
    articles:
      type: array
      items:
        $ref: '#/components/schemas/KnowledgeBaseArticle'
    error:
      $ref: '#/components/schemas/Error'
      description: Error information if operation failed
  description: Response payload for knowledge base search operation. Returns operation
    results and status.
