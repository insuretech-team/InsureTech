TicketCreationResponse:
  type: object
  properties:
    ticket_id:
      type: string
    ticket_number:
      type: string
  description: Response payload for ticket creation operation. Returns operation results
    and status.
