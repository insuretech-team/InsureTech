TicketRetrievalRequest:
  type: object
  properties:
    ticket_id:
      type: string
  description: Request payload for retrieving ticket retrieval information. May include
    filters and pagination parameters.
