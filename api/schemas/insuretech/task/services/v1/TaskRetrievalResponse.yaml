TaskRetrievalResponse:
  type: object
  properties:
    task:
      $ref: '#/components/schemas/Task'
  description: Response payload for task retrieval operation. Returns operation results
    and status.
