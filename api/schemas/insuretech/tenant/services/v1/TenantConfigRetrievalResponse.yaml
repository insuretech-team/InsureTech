TenantConfigRetrievalResponse:
  type: object
  properties:
    configs:
      type: array
      items:
        $ref: '#/components/schemas/TenantConfig'
  description: Response payload for tenant config retrieval operation. Returns operation
    results and status.
