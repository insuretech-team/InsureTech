TenantRetrievalResponse:
  type: object
  properties:
    tenant:
      $ref: '#/components/schemas/Tenant'
  description: Response payload for tenant retrieval operation. Returns operation
    results and status.
