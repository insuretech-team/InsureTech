TenantUpdateRequest:
  type: object
  properties:
    tenant_id:
      type: string
    name:
      type: string
    status:
      type: string
    config:
      type: string
      description: JSON
    branding:
      type: string
      description: JSON
  description: Request payload for updating an existing tenant update. Contains fields
    to be modified.
