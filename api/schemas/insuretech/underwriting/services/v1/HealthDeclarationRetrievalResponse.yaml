UnderwritingHealthDeclarationRetrievalResponse:
  type: object
  properties:
    health_declaration:
      $ref: '#/components/schemas/UnderwritingHealthDeclaration'
    error:
      $ref: '#/components/schemas/Error'
      description: Error information if operation failed
  description: Response payload for health declaration retrieval operation. Returns
    operation results and status.
