HealthDeclarationSubmissionRequest:
  type: object
  properties:
    quote_id:
      type: string
    height_cm:
      type: integer
      format: int32
    weight_kg:
      type: string
    has_pre_existing_conditions:
      type: boolean
    pre_existing_conditions:
      type: string
      description: JSON
    smoker:
      type: boolean
    alcohol_consumer:
      type: boolean
    occupation_risk_level:
      type: string
  description: Request payload for health declaration submission operation. Contains
    parameters required to execute the operation.
