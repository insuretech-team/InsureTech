UnderwritingQuotesListingResponse:
  type: object
  properties:
    quotes:
      type: array
      items:
        $ref: '#/components/schemas/Quote'
    total_count:
      type: integer
      format: int32
    error:
      $ref: '#/components/schemas/Error'
      description: Error information if operation failed
  description: Response payload for quotes listing operation. Returns operation results
    and status.
