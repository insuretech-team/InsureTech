UnderwritingRejectionResponse:
  type: object
  properties:
    decision_id:
      type: string
  description: Response payload for underwriting rejection operation. Returns operation
    results and status.
