VehicleRetrievalResponse:
  type: object
  properties:
    vehicle:
      $ref: '#/components/schemas/Vehicle'
  description: Response payload for vehicle retrieval operation. Returns operation
    results and status.
