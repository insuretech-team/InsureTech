VehicleUpdateResponse:
  type: object
  properties:
    vehicle:
      $ref: '#/components/schemas/Vehicle'
  description: Response payload for vehicle update operation. Returns operation results
    and status.
