EndVoiceSessionResponse:
  type: object
  properties:
    duration_seconds:
      type: integer
      format: int32
  description: Response payload for end voice session operation. Returns operation
    results and status.
