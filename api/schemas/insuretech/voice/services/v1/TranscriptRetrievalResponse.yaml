TranscriptRetrievalResponse:
  type: object
  properties:
    transcripts:
      type: array
      items:
        $ref: '#/components/schemas/VoiceTranscript'
  description: Response payload for transcript retrieval operation. Returns operation
    results and status.
