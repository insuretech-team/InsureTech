VoiceSessionRetrievalResponse:
  type: object
  properties:
    voice_session:
      $ref: '#/components/schemas/VoiceSession'
    commands:
      type: array
      items:
        $ref: '#/components/schemas/VoiceCommand'
  description: Response payload for voice session retrieval operation. Returns operation
    results and status.
