ConnectionStatsRetrievalRequest:
  type: object
  properties:
    room_id:
      type: string
    peer_id:
      type: string
  description: Request payload for retrieving connection stats retrieval information.
    May include filters and pagination parameters.
