ICECandidateSendingResponse:
  type: object
  properties:
    success:
      type: boolean
  description: Response payload for ice candidate sending operation. Returns operation
    results and status.
