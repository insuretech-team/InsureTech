TrackRetrievalResponse:
  type: object
  properties:
    track:
      $ref: '#/components/schemas/Track'
  description: Response payload for track retrieval operation. Returns operation results
    and status.
