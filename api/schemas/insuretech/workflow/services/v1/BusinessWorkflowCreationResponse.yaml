BusinessWorkflowCreationResponse:
  type: object
  properties:
    workflow:
      $ref: '#/components/schemas/BusinessWorkflowDefinition'
  description: Response payload for business workflow creation operation. Returns
    operation results and status.
