BusinessWorkflowRetrievalResponse:
  type: object
  properties:
    workflow:
      $ref: '#/components/schemas/BusinessWorkflowDefinition'
  description: Response payload for business workflow retrieval operation. Returns
    operation results and status.
