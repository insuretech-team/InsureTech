BusinessWorkflowsListingRequest:
  type: object
  properties:
    workflow_type:
      $ref: '#/components/schemas/BusinessWorkflowType'
    status:
      $ref: '#/components/schemas/BusinessWorkflowStatus'
    filter:
      type: string
    order_by:
      type: string
    page_size:
      type: integer
      format: int32
    page_token:
      type: string
  description: Request payload for listing business workflows listing items. Supports
    filtering, sorting, and pagination.
