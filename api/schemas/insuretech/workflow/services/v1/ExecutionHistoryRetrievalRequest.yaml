ExecutionHistoryRetrievalRequest:
  type: object
  properties:
    business_workflow_id:
      type: string
    entity_type:
      type: string
    entity_id:
      type: string
    page_size:
      type: integer
      format: int32
    page_token:
      type: string
  description: Request payload for retrieving execution history retrieval information.
    May include filters and pagination parameters.
