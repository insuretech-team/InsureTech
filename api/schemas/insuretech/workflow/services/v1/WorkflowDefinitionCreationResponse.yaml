WorkflowDefinitionCreationResponse:
  type: object
  properties:
    workflow_definition_id:
      type: string
    message:
      type: string
    error:
      $ref: '#/components/schemas/Error'
      description: Error information if operation failed
  description: Response payload for workflow definition creation operation. Returns
    operation results and status.
