WorkflowDefinitionRetrievalResponse:
  type: object
  properties:
    workflow_definition:
      $ref: '#/components/schemas/WorkflowDefinition'
    error:
      $ref: '#/components/schemas/Error'
      description: Error information if operation failed
  description: Response payload for workflow definition retrieval operation. Returns
    operation results and status.
