WorkflowHistoryRetrievalRequest:
  type: object
  properties:
    entity_type:
      type: string
    entity_id:
      type: string
  description: Request payload for retrieving workflow history retrieval information.
    May include filters and pagination parameters.
