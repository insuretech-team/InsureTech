WorkflowStartResponse:
  type: object
  properties:
    workflow_instance_id:
      type: string
  description: Response payload for workflow start operation. Returns operation results
    and status.
